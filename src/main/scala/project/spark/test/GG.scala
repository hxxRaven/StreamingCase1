package project.spark.test

class GG {
  def fegh(x: String, y: Int): String = {
    val str: String = y.toString()
    x + y
  }

  def  fv(x: Int, y: Int): Int = {
    x + y
  }

  val func = (x: Int, y: Int) => {

  }

}
